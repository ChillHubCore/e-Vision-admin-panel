import { lazy } from 'react';

export const SideNavigationPanel = lazy(() => import('./SideNavigationPanel'));
export const ShopSummary = lazy(() => import('./ShopSummary'));
export const UserGenerator = lazy(() => import('./UserGenerator'));
export const ProductGenerator = lazy(() => import('./ProductGenerator'));
